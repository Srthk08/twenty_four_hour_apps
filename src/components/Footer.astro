---
---

<style>
  /* ULTRA AGGRESSIVE FOOTER WHITE TEXT FIX */
  .footer, .footer *, .footer-content, .footer-content * {
    color: #ffffff !important;
    fill: #ffffff !important;
  }
  
  /* Force all text elements to be white */
  .footer p, .footer h1, .footer h2, .footer h3, .footer h4, .footer h5, .footer h6,
  .footer span, .footer div, .footer li, .footer ul, .footer ol {
    color: #ffffff !important;
  }
  
  /* Force all links to be white */
  .footer a, .footer a:visited, .footer a:link, .footer a:active {
    color: #ffffff !important;
  }
  
  .footer a:hover {
    color: #d1d5db !important;
  }
  
  /* Override ALL possible grey text classes */
  .footer .text-gray-100, .footer .text-gray-200, .footer .text-gray-300, 
  .footer .text-gray-400, .footer .text-gray-500, .footer .text-gray-600, 
  .footer .text-gray-700, .footer .text-gray-800, .footer .text-gray-900 {
    color: #ffffff !important;
  }
  
  /* Force all SVG elements to be white */
  .footer svg, .footer svg *, .footer svg path, .footer svg circle, .footer svg rect {
    fill: #ffffff !important;
    color: #ffffff !important;
    stroke: #ffffff !important;
  }
  
  /* Force all social media icons to be white */
  .footer .flex.space-x-4 svg, .footer .flex.space-x-4 svg * {
    fill: #ffffff !important;
    color: #ffffff !important;
  }
  
  /* Force company name and description to be white */
  .footer .text-xl, .footer .text-lg, .footer .text-sm, .footer .text-xs {
    color: #ffffff !important;
  }
  
  /* Force all headings to be white */
  .footer h1, .footer h2, .footer h3, .footer h4, .footer h5, .footer h6 {
    color: #ffffff !important;
  }
  
  /* Force all list items to be white */
  .footer li, .footer ul li, .footer ol li {
    color: #ffffff !important;
  }
  
  /* Force copyright and legal text to be white */
  .footer .text-sm, .footer .text-xs {
    color: #ffffff !important;
  }
  
  /* Override any Tailwind text color utilities */
  .footer [class*="text-"] {
    color: #ffffff !important;
  }
  
  /* Force all elements with any color style to be white */
  .footer [style*="color"] {
    color: #ffffff !important;
  }
</style>

<script>
  // ULTRA AGGRESSIVE JAVASCRIPT FOOTER WHITE TEXT FIX
  document.addEventListener('DOMContentLoaded', function() {
    function forceFooterWhite() {
      const footer = document.querySelector('.footer');
      if (footer) {
        // Force all text elements to be white
        const allElements = footer.querySelectorAll('*');
        allElements.forEach(element => {
          element.style.setProperty('color', '#ffffff', 'important');
          element.style.setProperty('fill', '#ffffff', 'important');
          element.style.setProperty('stroke', '#ffffff', 'important');
        });
        
        // Force all SVG elements to be white
        const svgElements = footer.querySelectorAll('svg, svg *');
        svgElements.forEach(svg => {
          svg.style.setProperty('fill', '#ffffff', 'important');
          svg.style.setProperty('color', '#ffffff', 'important');
          svg.style.setProperty('stroke', '#ffffff', 'important');
          svg.setAttribute('fill', '#ffffff');
        });
        
        // Force all links to be white
        const links = footer.querySelectorAll('a');
        links.forEach(link => {
          link.style.setProperty('color', '#ffffff', 'important');
        });
        
        // Force all text elements to be white
        const textElements = footer.querySelectorAll('p, h1, h2, h3, h4, h5, h6, span, div, li, ul, ol');
        textElements.forEach(text => {
          text.style.setProperty('color', '#ffffff', 'important');
        });
      }
    }
    
    // Run immediately
    forceFooterWhite();
    
    // Run after a short delay to catch any dynamically loaded content
    setTimeout(forceFooterWhite, 100);
    setTimeout(forceFooterWhite, 500);
    setTimeout(forceFooterWhite, 1000);
    
    // Run on any DOM changes
    const observer = new MutationObserver(function(mutations) {
      mutations.forEach(function(mutation) {
        if (mutation.type === 'childList' || mutation.type === 'attributes') {
          forceFooterWhite();
        }
      });
    });
    
    const footer = document.querySelector('.footer');
    if (footer) {
      observer.observe(footer, {
        childList: true,
        subtree: true,
        attributes: true,
        attributeFilter: ['style', 'class']
      });
    }

    // Handle footer product links with role-based access control
    function handleFooterProductLinks() {
      const footerProductLinks = document.querySelectorAll('.footer-product-link');
      
      footerProductLinks.forEach(link => {
        link.addEventListener('click', function(e) {
          e.preventDefault();
          
          const productSlug = this.getAttribute('data-product-slug');
          
          // Check if user is admin
          const isAdmin = checkIfUserIsAdmin();
          
          // If user is admin, allow access to all products
          if (isAdmin) {
            window.location.href = `/products/${productSlug}`;
            return;
          }
          
          // ALL products require authentication - check if user is logged in
          const authManager = window.globalAuthManager || window.simpleAuthManager;
          const isLoggedIn = authManager && authManager.isUserLoggedIn();
          
          if (!isLoggedIn) {
            // User not logged in - redirect to login
            console.log('❌ User not logged in - authentication required, redirecting to login');
            const loginUrl = `/login?redirect=/products/${productSlug}`;
            window.location.href = loginUrl;
            return;
          }
          
          // Check if this is the Order Menu System product - it should work for all users
          const isOrderMenuSystem = productSlug === 'order-menu-system';
          
          if (isOrderMenuSystem) {
            // Order Menu System - allow access for all logged-in users
            console.log('Order Menu System - allowing access for logged-in user');
            window.location.href = `/products/${productSlug}`;
            return;
          }
          
          // User is logged in but not admin - show dialog (except for OMS)
          if (window.showProductDialog) {
            window.showProductDialog();
          } else {
            window.location.href = '/contact';
          }
        });
      });
    }

    // Function to check if user is admin
    function checkIfUserIsAdmin() {
      try {
        // Check if user is logged in
        if (window.simpleAuthManager && window.simpleAuthManager.isUserLoggedIn()) {
          const currentUser = window.simpleAuthManager.getCurrentUser();
          
          // Check if user has admin role
          if (currentUser && currentUser.role === 'admin') {
            return true;
          }
        }
        
        // Check session storage for admin role
        const sessionData = sessionStorage.getItem('simple-auth-session');
        if (sessionData) {
          const session = JSON.parse(sessionData);
          if (session.user && session.user.role === 'admin') {
            return true;
          }
        }
        
        return false;
      } catch (error) {
        console.error('Error checking admin status:', error);
        return false;
      }
    }

    // Initialize footer product links
    handleFooterProductLinks();
    
    // Re-initialize when DOM changes (for dynamic content)
    const footerObserver = new MutationObserver(function(mutations) {
      mutations.forEach(function(mutation) {
        if (mutation.type === 'childList') {
          handleFooterProductLinks();
        }
      });
    });
    
    if (footer) {
      footerObserver.observe(footer, {
        childList: true,
        subtree: true
      });
    }
  });
</script>

<footer class="footer bg-gray-900 text-white" style="color: #ffffff !important;">
  <div class="footer-content max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      <!-- Company Info -->
      <div class="col-span-1 md:col-span-2">
        <div class="flex items-center space-x-2 mb-4">
          <div class="w-8 h-8 bg-blue-500 rounded-lg flex items-center justify-center">
            <span class="text-white font-bold text-sm">24</span>
          </div>
          <span class="text-xl font-bold text-white" style="color: #ffffff !important;">DevExpress</span>
        </div>
        <p class="text-white mb-4 max-w-md" style="color: #ffffff !important;">
          We deliver high-quality digital solutions in just 24 hours. From restaurant systems to mobile apps, 
          we're your trusted partner for rapid development.
        </p>
        <div class="flex space-x-4">
          <a href="#" class="text-white hover:text-gray-300 transition-colors" style="color: #ffffff !important;">
            <svg class="w-5 h-5" fill="#ffffff" viewBox="0 0 24 24" style="fill: #ffffff !important; color: #ffffff !important;">
              <path d="M24 4.557c-.883.392-1.832.656-2.828.775 1.017-.609 1.798-1.574 2.165-2.724-.951.564-2.005.974-3.127 1.195-.897-.957-2.178-1.555-3.594-1.555-3.179 0-5.515 2.966-4.797 6.045-4.091-.205-7.719-2.165-10.148-5.144-1.29 2.213-.669 5.108 1.523 6.574-.806-.026-1.566-.247-2.229-.616-.054 2.281 1.581 4.415 3.949 4.89-.693.188-1.452.232-2.224.084.626 1.956 2.444 3.379 4.6 3.419-2.07 1.623-4.678 2.348-7.29 2.04 2.179 1.397 4.768 2.212 7.548 2.212 9.142 0 14.307-7.721 13.995-14.646.962-.695 1.797-1.562 2.457-2.549z"/>
            </svg>
          </a>
          <a href="#" class="text-white hover:text-gray-300 transition-colors" style="color: #ffffff !important;">
            <svg class="w-5 h-5" fill="#ffffff" viewBox="0 0 24 24" style="fill: #ffffff !important; color: #ffffff !important;">
              <path d="M22.46 6c-.77.35-1.6.58-2.46.69.88-.53 1.56-1.37 1.88-2.38-.83.5-1.75.85-2.72 1.05C18.37 4.5 17.26 4 16 4c-2.35 0-4.27 1.92-4.27 4.29 0 .34.04.67.11.98C8.28 9.09 5.11 7.38 3 4.79c-.37.63-.58 1.37-.58 2.15 0 1.49.75 2.81 1.91 3.56-.71 0-1.37-.2-1.95-.5v.03c0 2.08 1.48 3.82 3.44 4.21a4.22 4.22 0 0 1-1.93.07 4.28 4.28 0 0 0 4 2.98 8.521 8.521 0 0 1-5.33 1.84c-.34 0-.68-.02-1.02-.06C3.44 20.29 5.7 21 8.12 21 16 21 20.33 14.46 20.33 8.79c0-.19 0-.37-.01-.56.84-.6 1.56-1.36 2.14-2.23z"/>
            </svg>
          </a>
          <a href="#" class="text-white hover:text-gray-300 transition-colors" style="color: #ffffff !important;">
            <svg class="w-5 h-5" fill="#ffffff" viewBox="0 0 24 24" style="fill: #ffffff !important; color: #ffffff !important;">
              <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
            </svg>
          </a>
        </div>
      </div>

      <!-- Quick Links -->
      <div>
        <h3 class="text-lg font-semibold mb-4 text-white" style="color: #ffffff !important;">Quick Links</h3>
        <ul class="space-y-2">
          <li><a href="/products" class="text-white hover:text-gray-300 transition-colors" style="color: #ffffff !important;">Our Products</a></li>
          <li><a href="/about" class="text-white hover:text-gray-300 transition-colors" style="color: #ffffff !important;">About Us</a></li>
          <li><a href="/contact" class="text-white hover:text-gray-300 transition-colors" style="color: #ffffff !important;">Contact</a></li>
          <li><a href="/faq" class="text-white hover:text-gray-300 transition-colors" style="color: #ffffff !important;">FAQ</a></li>
        </ul>
      </div>

      <!-- Services -->
      <div>
        <h3 class="text-lg font-semibold mb-4 text-white" style="color: #ffffff !important;">Services</h3>
        <ul class="space-y-2">
          <li><a href="#" class="text-white hover:text-gray-300 transition-colors footer-product-link" data-product-slug="restaurant-menu-system" style="color: #ffffff !important;">Restaurant Menu</a></li>
          <li><a href="#" class="text-white hover:text-gray-300 transition-colors footer-product-link" data-product-slug="order-menu-system" style="color: #ffffff !important;">Order Menu System</a></li>
          <li><a href="#" class="text-white hover:text-gray-300 transition-colors footer-product-link" data-product-slug="android-tv-app" style="color: #ffffff !important;">Android TV Apps</a></li>
          <li><a href="#" class="text-white hover:text-gray-300 transition-colors footer-product-link" data-product-slug="streaming-mobile-app" style="color: #ffffff !important;">Streaming Apps</a></li>
          <li><a href="#" class="text-white hover:text-gray-300 transition-colors footer-product-link" data-product-slug="restaurant-website" style="color: #ffffff !important;">Restaurant Websites</a></li>
        </ul>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="border-t border-gray-800 mt-8 pt-8 flex flex-col md:flex-row justify-between items-center">
      <p class="text-white text-sm" style="color: #ffffff !important;">
        © 2024 DevExpress. All rights reserved.
      </p>
      <div class="flex space-x-6 mt-4 md:mt-0">
        <a href="/privacy" class="text-white hover:text-gray-300 text-sm transition-colors" style="color: #ffffff !important;">Privacy Policy</a>
        <a href="/terms" class="text-white hover:text-gray-300 text-sm transition-colors" style="color: #ffffff !important;">Terms of Service</a>
      </div>
    </div>
  </div>
</footer>